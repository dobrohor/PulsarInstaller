<UserControl x:Class="PulsarInstaller.Views.Pages.WelcomePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="White" Margin="40">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding Title}" 
                       FontSize="32" 
                       FontWeight="Bold" 
                       Margin="0,0,0,20"
                       Foreground="#333333"/>
            
            <TextBlock Text="{Binding Description}" 
                       FontSize="14" 
                       Margin="0,0,0,40"
                       TextWrapping="Wrap"
                       Foreground="#666666"
                       MaxWidth="400"/>
            
            <StackPanel>
                <TextBlock Text="This wizard will guide you through the installation process." 
                           FontSize="12"
                           Margin="0,0,0,10"
                           Foreground="#888888"/>
                <TextBlock Text="Click 'Next' to continue." 
                           FontSize="12"
                           Foreground="#888888"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>

